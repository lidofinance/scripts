[tool.poetry]
name = "scripts"
version = "0.1.0"
description = "Repository for internal scripts."
authors = []
license = "MIT"

[tool.poetry.dependencies]
python = ">=3.8,<3.10"
eth-brownie = "~1.16.0"
avotes-parser-core = "0.4.3"


[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
